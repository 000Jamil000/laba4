#pragma once
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

bool inputMenu();

bool outputMenu();

string fileInput();

void fileOutput(string answer);

string answer2(string c);

string answer(string phrase);

bool repeat();